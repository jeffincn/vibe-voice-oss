# fluid-voice

Optional Vite/React preview of the particle voice HUD used during recording.

```bash
pnpm install
pnpm dev
```

This package is not required to build the macOS app under `Sources/VibeVoice`.
